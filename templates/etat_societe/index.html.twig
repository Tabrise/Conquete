{% extends 'base.html.twig' %}

{% block title %}EtatSociete index{% endblock %}

{% block body %}
    <h1>EtatSociete index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Logo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etat_societe in etat_societes %}
            <tr>
                <td>{{ etat_societe.id }}</td>
                <td>{{ etat_societe.nom }}</td>
                <td>{{ etat_societe.logo }}</td>
                <td>
                    <a href="{{ path('app_etat_societe_show', {'id': etat_societe.id}) }}">show</a>
                    <a href="{{ path('app_etat_societe_edit', {'id': etat_societe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_etat_societe_new') }}">Create new</a>
{% endblock %}
